{
  name = frame_linear
  filter = linear
  address = clamp
}
{
  // Глубина, нормали и motion читаются точечно: интерполировать их между пикселями нельзя
  name = frame_nearest
  filter = nearest
  address = clamp
}
