# GN01 — generator contract

**ЗАКРЫТА 2026-08-31** закрывающим аудитом: см. «Аудит при закрытии» в конце.

Первая площадка кампании генератора. Проверяет не «красивый мир», а устройство генератора:
типизированные буферы, шаги из конфига, lua как программируемую команду шага и воспроизводимость
результата на настоящем объёме данных.

Площадка headless: результат генератора — данные, а не картинка, поэтому он проверяется числами.

## Вопрос площадки

Где именно проходит граница между C++, `devils_script` и lua — и держится ли контракт
«параллельно == последовательно бит в бит», когда параллельность не сообщается скриптом, а выводится
из апертуры инструмента.

## Запуск

```bash
cmake --build build-release --target GN01_generator_contract -j4

# один прогон
./build-release/subprojects/playgrounds/GN01_generator_contract/bin/GN01_generator_contract --size=1024

# контрактные проверки
./build-release/subprojects/playgrounds/GN01_generator_contract/bin/GN01_generator_contract --verify --size=512

# замер уровней исполнения
./build-release/subprojects/playgrounds/GN01_generator_contract/bin/GN01_generator_contract --bench --size=2048
```

```bash
# сверить генерацию по NxN чанкам с картой целиком
./build-release/subprojects/playgrounds/GN01_generator_contract/bin/GN01_generator_contract \
  --chunked=4 --size=512
```

Ключи: `--size=N` (ширина квадратной сетки), `--sites=N` (число областей), `--chunked=N`,
`--layout=aos|soa`, `--threads=N`, `--seed=N`.

## Пайплайн

Пять шагов, объявленных в [`resources/steps.tavl`](resources/steps.tavl):

1. `terrain` — два независимых слоя FastNoise2 в `height` и `moisture`, дерево узлов приходит
   закодированной строкой из конфига; частота задаётся в чертах НА КАРТУ, а диапазон измеряется
   свёртками и нормализуется `remap` по месту;
2. `climate` — `box_blur` (апертура gather) в отдельное поле `smoothed`, затем нативная
   классификация биома;
3. `semantics` — то же правило классификации, но на `devils_script`
   ([`biome_rule.ds`](resources/scripts/biome_rule.ds)), в поле `biome_ds`; пороги приходят в скрипт
   как `ctx:arg:` из общих значений пайплайна;
4. `regions` — области: `voronoi_label` (gather) разметает клетки по ближайшему сайту,
   `voronoi_adjacency` (scatter) строит соседство из Делоне в CSR, `voronoi_polygons` (scatter) —
   контуры как планарную сетку с общими вершинами, `group_by` (scatter) раскладывает клетки по
   областям, `accumulate` (scatter) суммирует высоты по областям;
5. `stats` — две свёртки в буфер `state` с `size = 1`.

Тела шагов — lua в [`resources/scripts/`](resources/scripts). Ни одно из них не ходит по плотному
буферу: lua выбирает инструмент, поля и параметры. Исключение осознанное и разрешённое правилом —
позиции `256` сайтов областей скрипт расставляет поэлементно, потому что это множество он перечислил
сам.

Числа и строки, нужные нескольким шагам, лежат в [`resources/values.tavl`](resources/values.tavl) и
приходят каждому шагу среди его параметров. Это не косметика: `sea_level` читают нативная
классификация, правило на `devils_script` и подсчёт суши. Продублированный в трёх местах порог
однажды разъехался бы, и три уровня исполнения начали бы считать разное. Проверено вручную — правка
одной строки в `values.tavl` двигает долю суши с `75.77%` до `59.62%`, и трёхстороннее сравнение
остаётся зелёным.

Отдельный [`classify_lua.lua`](resources/scripts/classify_lua.lua) существует **ради замера** — он
повторяет нативную классификацию поэлементно и показывает, во что это обходится.

## Измерено

Iris Xe / 11 рабочих потоков, Release, сетка `2048x2048 = 4 194 304` элемента:

Сетка `2048x2048`, `1024` области.

| Работа | мс | нс/элемент |
|---|---|---|
| весь пайплайн, `soa`, 1 поток | 958.36 | 228.49 |
| весь пайплайн, `soa`, 11 потоков | 196.96 | 46.96 |
| весь пайплайн, `aos`, 1 поток | 959.72 | 228.81 |
| весь пайплайн, `aos`, 11 потоков | 206.72 | 49.29 |
| `noise_grid` (FastNoise2), 1 поток | 24.72 | 5.89 |
| `noise_grid` (FastNoise2), 11 потоков | 8.23 | 1.96 |
| `voronoi_label` (gather), 1 поток | 174.39 | 41.58 |
| `voronoi_label` (gather), 11 потоков | 29.77 | 7.10 |
| `voronoi_adjacency` (scatter), 1024 сайта | 0.53 | 521.60 на сайт |
| `voronoi_polygons` (scatter), 1024 сайта | 0.77 | 749.57 на сайт |
| `group_by` (scatter), 1 поток | 23.02 | 5.49 |
| `group_by` (scatter), 11 потоков | 5.18 | 1.24 |
| `accumulate` (scatter), 1 поток | 12.06 | 2.87 |
| `accumulate` (scatter), 11 потоков | 2.69 | 0.64 |
| классификация: нативное ядро, 1 поток | 14.43 | 3.44 |
| классификация: нативное ядро, 11 потоков | 4.62 | 1.10 |
| классификация: `devils_script`, пороги из конфига, 1 поток | 377.70 | 90.05 |
| классификация: `devils_script`, пороги из конфига, 11 потоков | 71.94 | 17.15 |
| классификация: `devils_script`, пороги литералами, 1 поток | 244.86 | 58.38 |
| классификация: поэлементный lua, 1 поток | 503.85 | 120.13 |

Что из этого следует:

- **Конфигурируемые пороги стоят 54%** на уровне `devils_script`: `90.05` против `58.38` нс.
  Литерал сворачивается на разборе, а `ctx:arg` читается со стека на каждом элементе. Размен
  сознательный: взамен порог живёт в конфиге в одном месте, и все три уровня исполнения читают одно
  и то же число. Если когда-нибудь прижмёт, эти 51% возвращает частичная подстановка известных
  параметров в программу при компиляции — но сначала должен появиться пайплайн, которому это мешает.
- **Оба scatter'а параллелятся** (4.6x и 4.2x) и при этом дают побитово тот же результат. Схема одна: фиксированное разбиение на чанки, у каждого чанка свой срез, слияние по
  номеру чанка. Порядок внутри группы и порядок сложения заданы номером чанка и индексом элемента,
  а не тем, кто когда успел.
- **`voronoi_label` дороже своей репутации:** 41.58 нс на клетку в один поток — это kd-запрос
  ближайшего сайта, и он в семь раз дороже самого шума (`5.89` нс). Зато он параллелится в 5.5 раза, а
  kd-дерево строится подготовкой один раз, а не в каждом чанке.
- **Мир перестал зависеть от разрешения.** Частота в чертах на карту плюс нормализация по измеренным
  min/max дают долю суши `75.77%` на сетке 256 и `75.96%` на 2048 при том же зерне. До этой правки
  фиксированная частота на клетку давала `2%`, `55%`, `80%` и `37%` на 256/512/1024/2048 — то есть
  разрешение молча меняло мир. Это ровно тот случай, ради которого дирижёру нужно читать данные:
  свёртка возвращает два числа, и по ним скрипт параметризует следующий вызов.

- **Главный сюрприз: выигрыш `devils_script` перед lua — не в скорости интерпретации.** В один
  поток разница всего двукратная (53.34 против 108.12). Значение имеет другое: ds
  **распараллеливается**, а поэлементный lua — нет, потому что интерпретатор один и однопоточный.
  Сравнивать надо `8.69` против `108.12`, и это уже 12 раз. Второе отличие — программа ds
  проверяется по типам НА РАЗБОРЕ, поэтому сломанное правило не доходит до исполнения вообще.
- **Числа согласуются с собственным замером проекта.** `script_vs_native_benchmark` даёт для ds
  `33` нс на простой предикат и `62` на выражение из четырёх операций; правило биома с двумя
  чтениями полей, тремя сравнениями и тремя вложенными `value_or` укладывается в `53.34` нс. То
  есть это обычная цена ds в этом проекте, а не накладные расходы originator.
- **Небезопасные опкоды удваивают скорость ds** (`105` → `53` нс/элемент). Проверка типов остаётся
  там, где ей место — на разборе; платить за неё на каждом из миллионов элементов незачем.
- **Граница lua числовая, а не вкусовая.** Тот же расчёт из lua дороже нативного в 35 раз в один
  поток. 4 млн элементов — это 504 мс, а 4 тысячи провинций — 0.48 мс. Поэтому правило звучит не
  «lua не трогает данные», а «lua обходит только те множества, которые сама перечислила».
- **Раскладка стоит немного, но стоит.** `aos` медленнее `soa` в пределах шума в один поток и на 5% на
  одиннадцати: при `soa` поле уходит в ядро непрерывным `span`, при `aos` — с шагом элемента, и на
  многих потоках разница в трафике памяти видна лучше. Ни один скрипт при переключении не меняется.
- **Замер поэлементного lua честный.** Предыдущие шаги отрабатывают по-настоящему: на нулевом буфере
  ветвление всегда уходило бы в самую дешёвую ветку и сравнивало бы разную работу.

## Что закреплено проверками

`--verify` — 17 проверок:

- полный пайплайн при 1, 3 и 7 потоках даёт **побайтово** те же ВСЕ буферы, что и последовательный,
  включая результаты обоих scatter'ов;
- свёртки не зависят от числа потоков (сравнение точное, не приближённое);
- `aos` и `soa` дают одинаковые значения по именам полей;
- нативная классификация, правило на `devils_script` и поэлементный lua-двойник совпадают на
  каждом элементе — одно правило, три уровня исполнения;
- gather с совпадающими источником и приёмником отклоняется **до** исполнения;
- чтение буфера до первой записи отклоняется при построении пайплайна;
- граф соседства областей симметричен и канонизирован (отсортирован, без повторов и петель);
- раскладка по областям покрывает каждую клетку ровно один раз, и клетка лежит в своей области;
- суммы `accumulate` сходятся и с прямым обходом группы, и с суммой по всему полю — два независимых
  пути дают одно число;
- контуры областей замощают карту: сумма площадей полигонов равна площади карты, каждое кольцо
  невырождено, обходится против часовой и канонизировано.

Отдельный запуск `--chunked=N` (тоже в ctest) проверяет независимость чанков и локальность
расхождения у gather.

## Что нашлось про FastNoise2

Два измеренных факта, которые пришлось учесть в обвязке, а не принять на веру.

**`GenUniformGrid2D` накапливает позиции внутри строки.** Полная строка бит в бит совпадает с одним
большим вызовом, а отрезок, начинающийся с середины строки, расходится примерно на `3e-07`. Поэтому
`noise_grid` генерирует строку ЦЕЛИКОМ независимо от того, где начался чанк, и пишет наружу только
его часть: лишняя работа — не больше двух краевых строк на чанк, зато результат не зависит от
разбиения по построению. `GenPositionArray2D/3D` этой проблемы не имеет — позиции заданы явно, и
любое разбиение даёт побитово одно и то же, что и проверяется тестом.

**Набор SIMD-инструкций выбирался в рантайме, и это меняло результат.** `max` против `SSE2` давало
расхождение того же порядка (`1.8e-07`): один и тот же seed давал разные артефакты на разных машинах.
Закрыто штатной опцией upstream — проект собирает FastNoise2 с `FASTNOISE2_STRICT_FP`, после чего
выход всех наборов побитово одинаков. Перемерено: расхождение ровно `0` и на регулярной сетке, и на
массиве позиций, а цена не различима на уровне пайплайна (`noise_grid` — `5.85` нс/элемент в один
поток и `2.01` на одиннадцати). Параметр `feature_set` остался диагностическим.

## Почему разметка, соседство и контуры — три разных инструмента

Три разных вопроса, и ни один не выводится из другого:

- `voronoi_label` — «какому сайту принадлежит эта клетка». Растровая разметка, запрос ближайшего.
- `voronoi_adjacency` — «какие области соседствуют». Из растра это плохо выводится: тонкая область
  может не получить ни одной клетки, а две области могут коснуться по диагонали. Поэтому соседство
  берётся из триангуляции Делоне, где ребро между сайтами и есть соседство по построению.
- `voronoi_polygons` — «какой у области контур». Отдаётся как **планарная сетка с общими вершинами**:
  соседние области ссылаются на одну вершину, а не на две совпадающие копии. Копии рано или поздно
  разъезжаются при любом преобразовании координат, и на стыке появляется щель, которой нет в данных.

Проверка контуров — самая сильная из возможных: сумма площадей полигонов равна площади карты. Она
ловит сразу и щели, и наложения, и неверный порядок обхода. Кольца канонизированы (начинаются с
минимального индекса вершины) и обходятся против часовой, поэтому сетку можно сравнивать побайтово
между запусками. На 64 областях получается `130` общих вершин и `356` углов, в среднем `5.56` на
область.

## Что чанкуется, а что нет

`--chunked=N` генерирует ту же карту как `NxN` чанков и сверяет с картой целиком. Вопрос здесь не
«работает ли чанкование», а **какие шаги его переживают** — ответ разный, и это главное содержание
режима. Карта `512x512`, `radius = 2`:

| Разбиение | pointwise (шум) | gather внутри чанка | gather в полосе границы |
|---|---|---|---|
| 2x2 | `0` на 262144 клетках | `0` на 258064 | `0.000766` на 4080 |
| 4x4 | `0` | `0` на 250000 | `0.00124` на 12144 |
| 8x8 | `0` | `0` на 234256 | `0.00139` на 27888 |

Третья строка вывода — про scatter. Шаг `regions` честно объявил свои группы глобальными
(`key_support = "global"`), поэтому при активном чанке его вызов **отклоняется до исполнения**, с
трейсбеком до конкретной строки скрипта. Это правильный исход: глобальную сводку из чанков не
собирают, её считают в грубом мировом проходе, где чанков нет.

То есть шум чанкуется **точно**, а размытие расходится **строго** в полосе шириной `radius` и ровно
нулевое внутри — значит чанкованному gather нужна полоса перекрытия шириной `radius`, и это не
догадка, а замер. Повторная генерация чанка `(0,0)` после всех остальных даёт `0`: чанк не зависит от
того, какие чанки посчитаны раньше, хотя буферы переиспользуются и не обнуляются.

Попутно выяснилась ошибка первоначального дизайна. Сначала ключ чанка входил в единственное зерно
шага — и шум получал в каждом чанке своё поле, расхождение `1.99`. Зёрен нужно **два**: `step.seed`
без ключа для того, что обязано быть непрерывным через границу, и `step.chunk_seed` с ключом для
того, что обязано быть независимым в каждом чанке. По умолчанию берётся непрерывное.

Второе следствие: **самокалибровка несовместима с чанкованием.** Шаг `terrain` меряет min/max и
нормализует по ним — для карты целиком это то, что делает мир независимым от разрешения, а для чанка
дало бы своё отображение в каждом чанке. Поэтому режим выбирает конфиг (`normalize` в `values.tavl`),
а не догадка скрипта, и в чанкованном прогоне обе стороны сравнения считаются с фиксированным
масштабом.

## Как devils_script видит буфер

Скоуп программы — **один элемент**. Поля видны по именам, объявленным в `buffers.tavl`, потому что
имена приходят от самих привязок: второго списка имён, который мог бы разъехаться с конфигом, не
существует. Привязка не тем полем отклоняется до исполнения.

Апертура здесь не объявляется, а **следует из регистрации**: скрипту доступны ровно два вида
обращений — прочитать поле своего элемента и вернуть значение. Функции, которая пишет или добирается
до соседа, в системе нет, поэтому программа структурно `pointwise` и её нельзя случайно сделать не
такой. Когда появится доступ к соседям, он приедет отдельной регистрацией — и вместе с ней апертура
станет `gather` со всеми её проверками.

Найденное ограничение синтаксиса: инфиксное сравнение внутри `condition` у `select` эта версия
`devils_script` не принимает (там ждут строчную форму и ищут функцию `<`), а `value_or` принимает его
как обычное булево выражение. Поэтому правило записано вложенными `value_or`.

## Граница площадки

Не входит: настоящая география, эрозия, история, запечатывание пакета на диск. Фоновый поток тоже не
входит и не должен: он принадлежит потребителю, а библиотека отвечает только за независимость чанков.
Fixture намеренно абстрактный — площадка отвечает за устройство генератора, а не за мир.

## Аудит при закрытии

Аудит выявил восемь пунктов; все закрыты, один из них — настоящий баг.

**1. Быстрый путь доступа к полю подменял тип (реальный баг).** `as_span<T>()` сверял только размер,
поэтому `span<float>` над полем, объявленным сырым `uint32`, проходил: `ui1` и `v1` оба
четырёхбайтовые. Инструмент писал туда биты float, и значение читалось обратно как `1061158912`
вместо `0.75` — молча, без единой жалобы. Достижимо через `noise_grid` и `value_noise`, то есть через
обычный путь генерации. Исправлено требованием **точного** совпадения рода хранения плюс
однокомпонентности; роды без точного C++-типа (`sf`, `c`) быстрого пути больше не имеют вовсе.
Проверка добавлена и падала до исправления.

**2. Предупреждения компилятора.** Именованная инициализация с опущенными полями давала 44
`-Wmissing-field-initializers`. Причина, а не симптом: у опциональных полей `tool_description` не было
инициализаторов по умолчанию. Теперь сборка библиотеки, площадки и тестов чистая.

**3. Мёртвая абстракция `binding_mode`.** Перечисление read/write осталось от раннего дизайна и ни
разу не использовалось: роль читателя и писателя выражена **типами** вью (`const_field_accessor` без
`set`), а не рантайм-тегом. Удалено — оставленное, оно предлагало бы неверную модель.

**4. Спекулятивный `originator.aperture_of`.** Lua-хелпер без правдоподобного потребителя: скрипту
незачем ветвиться по апертуре. Удалён. `tool_exists` оставлен, и теперь в коде написано почему —
примитивы это отдельная цель сборки, и спросить о доступности инструмента законно.

**5. Ставшие избыточными проверки.** Два инструмента проверяли `components == 1` перед быстрым путём;
после исправления №1 это делает сам `as_span`. Убрано.

**6. Не хватало громких проверок конфига.** Повтор буфера в `reads` или `writes` теперь ошибка
(иначе буфер дважды попадал в `published_after`), а объявленный и никем не привязанный буфер —
предупреждение с его размером: это оплаченная память, которой никто не пользуется.

**7. Набор инструментов ядра не был задокументирован** — только примитивы. Таблица добавлена, и
заодно объяснено, зачем рядом с FastNoise2 живёт простой `value_noise`: примитивы это отдельная цель,
и потребителю без внешних зависимостей нужно чем-то получить детерминированное поле.

**8. Незакрытый край разбора типа.** `parse_field_type("v0")` не был проверен тестом. Добавлено.

Не найдено: незакрытых `TODO`, расхождений между документацией и кодом после правок, утечек памяти в
пути буферов (аллокация одна на буфер, выравнивание проверено), гонок в путях `scatter` (обе схемы
дают побитовое совпадение при 1/4/9 потоках).

## Definition of Done

- [x] пайплайн описан в `tavl`, тела шагов — lua, ни один скрипт не обходит плотный буфер;
- [x] параллельно == последовательно бит в бит по всем буферам при разном числе потоков;
- [x] неверная привязка отклоняется до исполнения с внятным сообщением;
- [x] измерена стоимость трёх уровней исполнения на нескольких миллионах элементов;
- [x] `aos`/`soa` переключаются в конфиге без правки скриптов;
- [x] `devils_script` читает поля по именам привязок и пороги через `ctx:arg`;
- [x] инструменты `scatter` детерминированы при любом числе потоков и измерены;
- [x] шум и вороной идут из настоящих библиотек, кросс-машинный детерминизм закрыт `STRICT_FP`;
- [x] контуры областей — планарная сетка с общими вершинами, замощение проверено;
- [x] измерено, какие апертуры переживают чанкование и какая полоса перекрытия нужна gather;
- [x] глобальный `scatter` в чанковом проходе не собирается;
- [x] закрывающий аудит проведён, найденный баг исправлен и закреплён проверкой.

## Что площадка оставляет после себя

Ценность не в fixture — он абстрактный и удаляемый. Остаётся:

- библиотека [`libs/originator`](../../../libs/originator/README.md) с четырьмя целями сборки;
- `311` проверок в `ctest`, из них `44` про originator плюс два запуска площадки
  (`--verify`, `--chunked`);
- измеренные границы, а не мнения: три уровня исполнения, две раскладки, чанкование по апертурам,
  цена конфигурируемости порогов, согласованность двух путей шума;
- четыре зафиксированных правила, каждое из которых было ошибкой до замера: полные строки у
  регулярного шума, `STRICT_FP` вместо выбора набора SIMD в рантайме, два зерна вместо одного при
  чанковании, несовместимость самокалибровки с чанкованием.
