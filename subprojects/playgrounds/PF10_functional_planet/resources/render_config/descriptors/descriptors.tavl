{
  name = planet_data
  layout = [
    { resource = camera_buffer, usage = uniform, stage = "vertex | fragment" },
    { resource = surface_vertices, usage = storage_read, stage = vertex },
    { resource = political_atlas, usage = storage_read, stage = fragment },
    { resource = political_region_table, usage = storage_read, stage = fragment },
    { resource = surface_patches, usage = storage_read, stage = vertex }
  ]
}
{
  name = refined_planet_data
  layout = [
    { resource = camera_buffer, usage = uniform, stage = "vertex | fragment" },
    { resource = surface_vertices, usage = storage_read, stage = vertex },
    { resource = political_atlas, usage = storage_read, stage = fragment },
    { resource = political_region_table, usage = storage_read, stage = fragment },
    { resource = refined_surface_patches, usage = storage_read, stage = vertex }
  ]
}
{
  name = marker_data
  layout = [
    { resource = camera_buffer, usage = uniform, stage = "vertex | fragment" },
    { resource = landmarks, usage = storage_read, stage = vertex }
  ]
}
{
  name = label_data
  layout = [
    { resource = camera_buffer, usage = uniform, stage = "vertex | fragment" },
    { resource = label_glyphs, usage = storage_read, stage = vertex },
    { resource = scene_depth, usage = depth_read, sampler = decal_nearest, stage = fragment },
    { resource = political_atlas, usage = storage_read, stage = fragment },
    { resource = political_region_table, usage = storage_read, stage = fragment }
  ]
}
{
  name = hydrology_data
  layout = [
    { resource = camera_buffer, usage = uniform, stage = vertex },
    { resource = hydrology_features, usage = storage_read, stage = vertex }
  ]
}
{ name = camera_data, layout = [ { resource = camera_buffer, usage = uniform, stage = vertex } ] }
{ name = ui_textures, texture_count = 16, sampler_pool = [ ui_linear ], texture_stage = fragment }
