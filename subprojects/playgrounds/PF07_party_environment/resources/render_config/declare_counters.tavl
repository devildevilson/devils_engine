per_frame
per_update
swapchain
// Условный счётчик: его двигает ХОСТ при смене параметров среды. Таблица многократного рассеяния
// зависит ТОЛЬКО от них — зенитный угол светила является её осью, а не аргументом, поэтому со временем
// суток она не меняется вообще, и пересчитывать её каждый кадр незачем.
atmosphere_cache
