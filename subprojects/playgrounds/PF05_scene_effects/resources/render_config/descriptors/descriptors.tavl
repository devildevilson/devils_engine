{
  name = camera_data
  layout = [ { resource = camera_buffer, usage = uniform, stage = all } ]
}
{
  // The same asset array contains the Crimson atlas used by world text and the Visage overlay.
  name = text_textures
  texture_count = 16
  sampler_pool = [ linear_sampler ]
  texture_stage = fragment
}
